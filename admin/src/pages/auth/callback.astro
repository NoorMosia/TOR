---
// This page is no longer the primary auth flow.
// Login now uses OTP code entry directly in /login.
// This callback is kept as a fallback — redirect to login with a helpful message.
return Astro.redirect('/login?error=use_code');
---
